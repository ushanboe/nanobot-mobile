# Nanobot Environment Variables
# Copy this to .env and fill in your values

# LLM API Keys (at least one required)
ANTHROPIC_API_KEY=your-anthropic-key-here
OPENAI_API_KEY=your-openai-key-here

# Default model (optional)
NANOBOT_DEFAULT_MODEL=claude-sonnet-4-20250514

# Database (Railway provides DATABASE_URL automatically if you add Postgres)
# DATABASE_URL=postgresql://user:pass@host:5432/db

# Server config
PORT=8080

# Gmail OAuth
# Step 1: Download OAuth keys from Google Cloud Console > APIs & Services > Credentials (Desktop app type)
GMAIL_OAUTH_KEYS_JSON={"installed":{"client_id":"xxx.apps.googleusercontent.com","client_secret":"xxx",...}}
# Step 2: Run `node setup-gmail-token.js` locally — it opens a browser for OAuth consent
# Step 3: Copy the token JSON output here (NOT the keys file — must have refresh_token!)
GMAIL_CREDENTIALS_JSON={"access_token":"ya29.xxx","refresh_token":"1//xxx","scope":"https://www.googleapis.com/auth/gmail.modify","token_type":"Bearer","expiry_date":1700000000000}

# Microsoft 365 (from Azure Portal > App Registrations)
# Required permissions: Mail.Read, Mail.Send, Files.Read, Files.ReadWrite
MS365_MCP_CLIENT_ID=your-azure-app-client-id
MS365_MCP_CLIENT_SECRET=your-azure-client-secret
MS365_MCP_TENANT_ID=common
# Step: Run `node setup-ms365-token.js` locally to complete device code login
# Copy the token cache output here (persists auth across Railway deploys)
MS365_TOKEN_CACHE_JSON={"AccessToken":{},"RefreshToken":{},...}
MS365_SELECTED_ACCOUNT_JSON={"homeAccountId":"...","environment":"...","username":"..."}
